/** @type {import('next').NextConfig} */
const nextConfig = {
  // Next.js 14+ has App Router enabled by default
  // Remove dev warning for cross-origin requests in Replit
}

module.exports = nextConfig
